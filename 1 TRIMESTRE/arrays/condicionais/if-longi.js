// verifica se LOGING e SENHA
// correspondem ao mesmo tempo (&&)

const login = 'superman'
const senha = 'maracujina56'
 
if (login == 'superman' && senha == 'maracujino56') {
    console.log('aceso permitido!')
} else {
    console.warn('verifique seus dados')
}